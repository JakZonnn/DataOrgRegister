1.多选的字段需要用List<String>来存 √
2.学科分类，国家/省/市 这种给用户选择的需要用枚举
3.有重复字段的表，比如主任副主任，可以写一个type，然后剩下的重复字段可以直接命名，不需要分类  √
4.是否有国家的名单
5.数据库数据量	PB/TB/GB/MB
6.注意日期格式 √


疑问：
1.请列示如何实现,是不是网页上点击下拉展示,如果是，代码需修改
2.认证与收录中：国内项目支持的数据库，列示项目名称，如何实现
3.logo存图片格式, 思路：图片存到服务器一个路径下，mongodb中存logo的路径，先拿到logo的路径再用set方法将路径写入logo字段
4.数据量应该是double吧？会有小数点，单位PB/TB/GB/MB如何实现, 是不是前端单位，后端接受数据然后做校验，统一成字节单位存入数据库,  方法：写个枚举，加入单位
5.学科分类和所在地 怎么存数据
6.personinfodb中的字段，如何能存多个主任和人员

检查：
1.suject和location字段的存储方式

9.14
1.学科分类的接口还没写，目前返回的都是null，参照dataspace写
2.选项中的其他，需要点击之后写东西
3.点击认证与收录之后，填写加入认证的时间和更新时间
4.验证码校验是前端需要调用的接口
5.先做db和dc注册
